{
    "name": "appscript-dev",
    "dockerFile": "Dockerfile",
    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": [
                "eamodio.gitlens"
            ]
        }
    },
    "postCreateCommand": "apt-get update && apt-get install -y git; npm install -g @google/clasp; npm i -S @types/google-apps-script"
}